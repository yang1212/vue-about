JS拥有许多的设计模式，常见的模式有单例模式，代理模式等，设计模式的合理运用不仅使代码结构更加清晰，也便于后期的维护与重构。
然而在日常的学习中，我们却在不经意间使用了单例模式，but没有认出来，额。。。应该是我，不是我们。

1  什么是单例模式？

限制一个类仅有一个实例化对象，（但js本身是一种“无类”语言）并提供一个访问它的全局访问点

经典的实现方式是，创建一个类，这个类包含一个方法，这个方法在没有对象存在的情况下，将会创建一个新的实例对象。如果对象存在，这个方法只是返回这个对象的引用

2  用于什么场景，怎么用？





3  优缺点

4  比较