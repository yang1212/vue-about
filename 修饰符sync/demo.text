1 修饰符sync的是：

父子组件数据双向绑定的一种语法糖,实现可以在子组件改变prop的值。
只可通过这种方式修改this.$emit('update:show', false);，其他在2.0都会报错

2 缺点和优点？

从好处上看： 
1.它实现了父子组件数据的“实时”同步， 在某些数据场景下可能会使用到这一点
2.sync提供的语法糖使得双向绑定的代码变得很简单

从坏处上看：
它破环了单向数据流的简洁性， 这增加了分析数据时的难度

3 sync实现双向绑定与v-model的区别？
