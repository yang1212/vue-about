1 修饰符sync的是：
    父子组件数据双向绑定的一种语法糖
    当一个组件改变了一个Prop的值时，这个变化也会同步到父组件中所绑定。
    Prop的值可以改变吗？
    只可通过这种方式修改this.$emit('update:show', false);，其他在2.0都会报错

    如果我们不用.sync，想做弹窗功能，我们也可以props传初始值，
    然后事件监听，实现起来也不算复杂。

2 修饰符sync的运用场景是？

3 缺点和优点？
从好处上看： 
1.它实现了父子组件数据的“实时”同步， 在某些数据场景下可能会使用到这一点
2.sync提供的语法糖使得双向绑定的代码变得很简单

从坏处上看：
它破环了单向数据流的简洁性， 这增加了分析数据时的难度

4 sync实现双向绑定与v-model的区别？
